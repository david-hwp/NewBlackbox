<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Required to bind to the Engine Service (com.zhirang.zhanghaoguanjia.engine) -->
    <uses-permission android:name="com.zhirang.zhanghaoguanjia.engine.permission.BIND_ENGINE" />

    
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Microphone / Audio -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Phone -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Contacts -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <!-- SMS -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- Bluetooth (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Sensors -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />


    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BlackBox"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="n">
        <activity
            android:name=".view.fake.FollowMyLocationOverlay"
            android:exported="false" />
        <activity android:name=".view.setting.SettingActivity" />
        <activity android:name=".view.gms.GmsManagerActivity" />

        <!-- 多店管家新 Activity -->
        <activity
            android:name=".view.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Duodian">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.home.HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.Duodian" />
        <activity
            android:name=".view.profile.ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.Duodian" />
        <activity
            android:name=".view.gift.GiftActivity"
            android:exported="false"
            android:theme="@style/Theme.Duodian" />
        <activity
            android:name=".view.logs.LogsActivity"
            android:exported="false"
            android:theme="@style/Theme.Duodian" />
        <activity
            android:name=".view.profile.EngineSwitchActivity"
            android:exported="false"
            android:theme="@style/Theme.Duodian" />
        <activity
            android:name=".view.profile.FeedbackActivity"
            android:exported="false"
            android:theme="@style/Theme.Duodian" />

        <!-- Engine Install Activity: Entry point for first-time setup -->
        <activity
            android:name=".view.splash.EngineInstallActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.BlackBox" />

        <activity
            android:name=".view.main.WelcomeActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/WelcomeTheme" />
        <activity android:name=".view.list.ListActivity" />
        <activity android:name=".view.fake.FakeManagerActivity" />

        <activity
            android:name=".view.main.ShortcutActivity"
            android:excludeFromRecents="true"
            android:exported="true" />
        <activity android:name=".view.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <!-- Engine Install Receiver: Listens for Engine package add/replace events -->
        <receiver
            android:name=".engine.EngineInstallReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for sharing APK files during installation -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
