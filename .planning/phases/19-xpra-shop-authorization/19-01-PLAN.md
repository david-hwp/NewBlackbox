---
phase: 19-xpra-shop-authorization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/layout/item_shop_card.xml
  - app/src/main/java/com/zhirang/zhanghaoguanjia/view/home/ShopListAdapter.kt
  - app/src/main/java/com/zhirang/zhanghaoguanjia/view/dialog/ShopAuthorizationSheetFragment.kt
  - admin/backend/src/main/java/com/duodian/admin/controller/PlatformController.java
  - admin/backend/src/main/java/com/duodian/admin/config/SoftDeleteSchemaInitializer.java
  - admin/frontend/src/views/Platforms.vue
  - admin/scripts/browser/start-zr.sh
  - admin/scripts/browser/start-zr-browser.sh
  - admin/scripts/browser/start-zr-display.sh
  - admin/scripts/browser/zr-browser-control.py
autonomous: true
requirements:
  - PHASE-19-WAVE-1-MVP
---

<objective>
Create the first usable shop authorization MVP: the Android shop card opens a "店铺授权" WebView sheet, the admin side can configure a platform authorization URL, and the remote browser server starts an isolated Xvfb/Chromium/Xpra session for the requested user and shop profile.
</objective>

<execution_context>
@/Users/hwp/.codex/gsd-core/workflows/execute-plan.md
@.planning/phases/19-xpra-shop-authorization/19-PLAN.md
@.planning/phases/19-xpra-shop-authorization/19-LEGACY-PLAN.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Add Android shop authorization entry</name>
  <files>
    app/src/main/res/layout/item_shop_card.xml
    app/src/main/java/com/zhirang/zhanghaoguanjia/view/home/ShopListAdapter.kt
    app/src/main/java/com/zhirang/zhanghaoguanjia/view/dialog/ShopAuthorizationSheetFragment.kt
  </files>
  <action>
    Add a visible "授权登录该店铺" entry on each shop card. Clicking it opens a modal WebView titled only "店铺授权"; the modal must not trigger private-traffic billing or advanced-feature flows. Before loading Xpra, the APP calls the ZR control service `/open` with phone, shop id, authorization URL, and the WebView viewport dimensions.
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :app:compileDebugKotlin</automated>
  </verify>
  <done>
    - Shop card contains the authorization entry.
    - The entry opens a WebView sheet with the expected title.
    - APP passes width, height, scale, phone, shop id, and URL to the control service before loading stream HTML.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add platform authorization URL management</name>
  <files>
    admin/backend/src/main/java/com/duodian/admin/config/SoftDeleteSchemaInitializer.java
    admin/backend/src/main/java/com/duodian/admin/controller/PlatformController.java
    admin/frontend/src/views/Platforms.vue
  </files>
  <action>
    Add `platforms.authorization_url` and expose it through backend DTOs and the support-platform admin page. This is a platform login URL template, not a shop-specific Xpra stream URL.
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) mvn test</automated>
    <automated>cd admin/frontend &amp;&amp; npm run build</automated>
  </verify>
  <done>
    - Existing databases get the column on startup.
    - Super admin can edit the platform authorization URL.
    - APP and backend never display the platform URL inside the authorization sheet.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create remote display and browser control scripts</name>
  <files>
    admin/scripts/browser/start-zr.sh
    admin/scripts/browser/start-zr-display.sh
    admin/scripts/browser/start-zr-browser.sh
    admin/scripts/browser/zr-browser-control.py
  </files>
  <action>
    Implement the remote server scripts that start Xvfb, fluxbox, Xpra HTML5, VNC, and Chromium. `/open` must create or reuse a profile under `profiles/&lt;phone&gt;/&lt;shopId&gt;/chrome`, size the virtual desktop to the requested viewport, open the platform authorization URL, and return the Xpra stream URL.
  </action>
  <verify>
    <automated>bash -n admin/scripts/browser/start-zr.sh admin/scripts/browser/start-zr-display.sh admin/scripts/browser/start-zr-browser.sh</automated>
    <automated>python3 -m py_compile admin/scripts/browser/zr-browser-control.py</automated>
  </verify>
  <done>
    - `/health` returns ok.
    - `/open` returns `ok=true`, stream port, profile path, and stream URL.
    - Xpra HTML5 and VNC are reachable for manual observation.
  </done>
</task>
</tasks>

<verification>
- `git diff --check`
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :app:assembleDebug --no-daemon`
- `bash -n admin/scripts/browser/start-zr.sh admin/scripts/browser/start-zr-display.sh admin/scripts/browser/start-zr-browser.sh`
- `python3 -m py_compile admin/scripts/browser/zr-browser-control.py`
- Manual: tap "授权登录该店铺" on a test phone and confirm the Xpra page shows the remote Chromium authorization page.
</verification>

<success_criteria>
- Android can open a shop-specific authorization sheet without touching private-traffic feature logic.
- Admin can configure platform authorization URLs.
- The remote browser server can create isolated user/shop profiles and return usable Xpra HTML5 streams.
- Xpra and VNC are both available for debugging the same session.
</success_criteria>

<output>
Create `.planning/phases/19-xpra-shop-authorization/19-01-SUMMARY.md` after execution.
</output>
