---
phase: 11-channel-promotion-system
plan: 02
type: execute
status: planned
created_at: "2026-06-10T00:45:00+08:00"
branch: dev
wave: 2
depends_on:
  - .planning/phases/11-channel-promotion-system/11-01-PLAN.md
files_modified:
  - app/build.gradle
  - Bcore/build.gradle
  - app/src/main/AndroidManifest.xml
  - Bcore/src/main/AndroidManifest.xml
  - app/src/main/res/values/strings.xml
  - Bcore/src/main/res/values/strings.xml
  - app/src/main/java/com/zhirang/zhanghaoguanjia/core/EngineInstaller.kt
  - app/src/main/java/com/zhirang/zhanghaoguanjia/core/EngineConnection.kt
  - app/src/main/java/com/zhirang/zhanghaoguanjia/core/EngineLoader.kt
  - app/src/main/java/com/zhirang/zhanghaoguanjia/core/EnginePermissionCenter.kt
  - Bcore/src/main/java/top/niunaijun/blackbox/BlackBoxCore.java
  - Bcore/src/main/java/top/niunaijun/blackbox/core/env/BEnvironment.java
  - Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/CloneInstanceStore.java
  - Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/CloneInstanceMetadata.java
  - Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/BPackageManagerService.java
  - Bcore/src/main/java/top/niunaijun/blackbox/core/system/user/BUserManagerService.java
autonomous: false
requirements:
  - CHANNEL-PACKAGE-ID-01
  - CHANNEL-APP-CLIENT-01
  - CHANNEL-ENGINE-VERSION-01
must_haves:
  truths:
    - "Main channel package names remain unchanged."
    - "Channel APK package identity is configured through Gradle parameters, not source edits per channel."
    - "Main APK resolves engine package from BuildConfig or manifest placeholders."
    - "Engine data roots derive from the installed engine package/private data directory."
    - "Changing applicationId isolates Android package data directories for future channel packages."
  artifacts:
    - path: "app/build.gradle"
      provides: "Parameterized app identity and BuildConfig channel constants"
      exports: ["BuildConfig.APK_CHANNEL", "BuildConfig.ENGINE_PACKAGE"]
    - path: "Bcore/build.gradle"
      provides: "Parameterized engine identity and channel constants"
      exports: ["BuildConfig.APK_CHANNEL"]
    - path: "app/src/main/AndroidManifest.xml"
      provides: "Engine package, permission, and provider placeholders"
    - path: "Bcore/src/main/java/top/niunaijun/blackbox/core/env/BEnvironment.java"
      provides: "Engine-private data root resolution"
      exports: ["BEnvironment"]
  key_links:
    - from: "app Gradle params"
      to: "EngineInstaller and EngineConnection"
      via: "BuildConfig.ENGINE_PACKAGE"
    - from: "Bcore applicationId"
      to: "BEnvironment"
      via: "context package/private files directory"
    - from: "manifestPlaceholders"
      to: "provider authorities and bind permissions"
      via: "channel-specific identifiers"
---

<objective>
Parameterize main APK and engine APK package identity so Phase 11 can build isolated channel packages, while preserving `main` package names and avoiding hardcoded BlackBox or old engine package references in app/engine communication and data roots.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-channel-promotion-system/11-CONTEXT.md
@.planning/phases/11-channel-promotion-system/11-RESEARCH.md
@.planning/phases/11-channel-promotion-system/11-DISCUSSION-NOTES.md
@.planning/phases/11-channel-promotion-system/11-01-PLAN.md
</context>

<threat_model>
  <assets>
    <asset>Installed main APK and engine APK package identity.</asset>
    <asset>Engine service binding, provider authority, install/update flow, and notification identity.</asset>
    <asset>Per-channel Android package data directories and engine virtual data roots.</asset>
    <asset>Existing main-channel user and shop data.</asset>
  </assets>
  <risks>
    <risk>Hardcoded old package names could cause channel APKs to bind to the wrong engine.</risk>
    <risk>Provider authorities or permissions could collide across channel packages.</risk>
    <risk>Changing data roots incorrectly could break existing main-channel engine data.</risk>
    <risk>Engine package rename could leave BlackBox strings visible or break permission center behavior.</risk>
  </risks>
  <mitigations>
    <mitigation>Keep `main` package identifiers unchanged and test before non-main channel builds.</mitigation>
    <mitigation>Make engine package, bind permission, provider authority, and notification labels Gradle placeholders.</mitigation>
    <mitigation>Audit all app-side hardcoded engine package references and replace them with `BuildConfig.ENGINE_PACKAGE` or a single resolver.</mitigation>
    <mitigation>Derive engine storage roots from the installed engine context/private directory and avoid shared fixed public roots.</mitigation>
    <mitigation>Add device smoke tests for install, bind, permission center, engine startup, and existing shop launch.</mitigation>
  </mitigations>
</threat_model>

<tasks>

<task id="11-02-T01" type="auto">
  <name>Task 1: Add Gradle parameters for app, engine, channel, and display identity</name>
  <files>
    app/build.gradle
    Bcore/build.gradle
    app/src/main/res/values/strings.xml
    Bcore/src/main/res/values/strings.xml
  </files>
  <read_first>
    settings.gradle
    build.gradle
    app/build.gradle
    Bcore/build.gradle
    app/src/main/res/values/strings.xml
    Bcore/src/main/res/values/strings.xml
  </read_first>
  <action>
    Add build parameters with safe defaults for the main channel:
    - `DUODIAN_APK_CHANNEL=main`
    - `DUODIAN_APP_APPLICATION_ID=com.zhirang.zhanghaoguanjia`
    - `DUODIAN_ENGINE_APPLICATION_ID=com.zhirang.zhanghaoguanjia.engine`
    - `DUODIAN_APP_NAME=店铺管家`
    - `DUODIAN_ENGINE_NAME=店铺管家引擎`

    Apply `DUODIAN_APP_APPLICATION_ID` to the app module `applicationId` and `DUODIAN_ENGINE_APPLICATION_ID` to the engine module `applicationId`. Generate `BuildConfig.APK_CHANNEL`, `BuildConfig.ENGINE_PACKAGE`, app name, engine name, and any release worker metadata needed by later waves. Keep defaults equivalent to the current production main package so existing installs upgrade in place.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug :Bcore:assembleDebug</automated>
    <manual>Inspect merged manifests or APK badging and confirm main defaults produce the existing package ids and display names.</manual>
  </verify>
  <acceptance_criteria>
    - Main default app application id remains `com.zhirang.zhanghaoguanjia`.
    - Main default engine application id remains `com.zhirang.zhanghaoguanjia.engine`.
    - BuildConfig exposes app channel and engine package.
    - App and engine display names are parameterized without source edits.
    - Build succeeds without passing any channel-specific Gradle properties.
  </acceptance_criteria>
</task>

<task id="11-02-T02" type="auto">
  <name>Task 2: Parameterize manifests, provider authorities, and engine bind permissions</name>
  <files>
    app/src/main/AndroidManifest.xml
    Bcore/src/main/AndroidManifest.xml
    app/build.gradle
    Bcore/build.gradle
  </files>
  <read_first>
    app/src/main/AndroidManifest.xml
    Bcore/src/main/AndroidManifest.xml
    app/build.gradle
    Bcore/build.gradle
  </read_first>
  <action>
    Replace hardcoded package-linked manifest strings with placeholders derived from the app and engine package ids. Cover service actions, bind permissions, provider authorities, FileProvider authorities, installer/update authorities, and any permission name that includes the old package.

    Ensure channel packages cannot collide with `main` provider authorities or engine bind permissions. Keep main placeholders resolving to current values so installed main APK/engine remain compatible.
  </action>
  <verify>
    <automated>./gradlew :app:processDebugMainManifest :Bcore:processDebugMainManifest</automated>
    <manual>Open merged manifests and verify no provider authority or custom permission remains fixed to an incompatible old package for channel builds.</manual>
  </verify>
  <acceptance_criteria>
    - Main merged manifests keep existing production identifiers.
    - Channel merged manifests use channel-specific authorities and permissions.
    - Main APK can bind only to its configured engine package.
    - Provider authority collisions do not occur when main and a channel package are installed together.
  </acceptance_criteria>
</task>

<task id="11-02-T03" type="auto">
  <name>Task 3: Replace app-side hardcoded engine package references</name>
  <files>
    app/src/main/java/com/zhirang/zhanghaoguanjia/core/EngineInstaller.kt
    app/src/main/java/com/zhirang/zhanghaoguanjia/core/EngineConnection.kt
    app/src/main/java/com/zhirang/zhanghaoguanjia/core/EngineLoader.kt
    app/src/main/java/com/zhirang/zhanghaoguanjia/core/EnginePermissionCenter.kt
  </files>
  <read_first>
    app/src/main/java/com/zhirang/zhanghaoguanjia/core/EngineInstaller.kt
    app/src/main/java/com/zhirang/zhanghaoguanjia/core/EngineConnection.kt
    app/src/main/java/com/zhirang/zhanghaoguanjia/core/EngineLoader.kt
    app/src/main/java/com/zhirang/zhanghaoguanjia/core/EnginePermissionCenter.kt
    app/src/main/java/com/zhirang/zhanghaoguanjia/data/VersionRepository.kt
  </read_first>
  <action>
    Create or reuse a single app-side resolver for engine package identity, based on `BuildConfig.ENGINE_PACKAGE`. Update engine install detection, version comparison, checksum validation request payloads, permission center launch, service bind intents, broadcast actions, and installer flows to use this resolver.

    Preserve checksum and versionCode server verification. The only behavior change in this task is package identity selection; do not weaken package integrity checks.
  </action>
  <verify>
    <automated>rg \"com\\.zhirang\\.zhanghaoguanjia\\.engine|top\\.niunaijun\\.blackbox|BlackBox\" app/src/main/java</automated>
    <automated>./gradlew :app:assembleDebug</automated>
    <manual>Install main app on Xiaomi real device and verify engine detection, permission center, and bind flow still work.</manual>
  </verify>
  <acceptance_criteria>
    - No app-side engine operation depends on a duplicated hardcoded package constant.
    - Existing main engine is still detected as installed.
    - Engine upgrade/download verification still sends versionCode and MD5/SHA-256.
    - Engine permission center still opens the configured engine package.
    - User-facing BlackBox strings are not introduced.
  </acceptance_criteria>
</task>

<task id="11-02-T04" type="auto">
  <name>Task 4: Make engine data roots and clone metadata package-aware</name>
  <files>
    Bcore/src/main/java/top/niunaijun/blackbox/BlackBoxCore.java
    Bcore/src/main/java/top/niunaijun/blackbox/core/env/BEnvironment.java
    Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/CloneInstanceStore.java
    Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/CloneInstanceMetadata.java
    Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/BPackageManagerService.java
    Bcore/src/main/java/top/niunaijun/blackbox/core/system/user/BUserManagerService.java
  </files>
  <read_first>
    Bcore/src/main/java/top/niunaijun/blackbox/core/env/BEnvironment.java
    Bcore/src/main/java/top/niunaijun/blackbox/BlackBoxCore.java
    Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/CloneInstanceStore.java
    Bcore/src/main/java/top/niunaijun/blackbox/core/system/user/BUserManagerService.java
  </read_first>
  <action>
    Audit Bcore data directory creation and clone metadata storage. Remove assumptions that the engine package is a fixed old package or that all channel builds share a fixed public root. Derive roots from the running engine context package/private data directory.

    Preserve the Phase 9 rule that platform app anti-sandbox-visible files must not be placed inside the cloned third-party app's data directory. Clone metadata/token mapping remains under the engine-controlled clone id directory, not under the platform app directory.
  </action>
  <verify>
    <automated>./gradlew :Bcore:assembleDebug</automated>
    <manual>Install a channel-parameterized debug engine next to main in a test environment and confirm data directories do not overlap.</manual>
  </verify>
  <acceptance_criteria>
    - Engine runtime roots derive from the installed engine package/private path.
    - Main channel existing data path remains compatible.
    - Channel engine builds do not share main engine virtual data roots.
    - Clone metadata remains outside cloned third-party app directories.
    - Existing logged-in shop clone launch still works on main after this change.
  </acceptance_criteria>
</task>

<task id="11-02-T05" type="auto">
  <name>Task 5: Smoke-test package identity and engine compatibility</name>
  <files>
    app/build.gradle
    Bcore/build.gradle
    app/src/main/java/com/zhirang/zhanghaoguanjia/core/EngineInstaller.kt
    app/src/main/java/com/zhirang/zhanghaoguanjia/core/EngineConnection.kt
    Bcore/src/main/java/top/niunaijun/blackbox/core/env/BEnvironment.java
  </files>
  <read_first>
    CLAUDE.md
    .planning/phases/10-engine-permission-center/10-PLAN.md
    .planning/phases/11-channel-promotion-system/11-02-PLAN.md
  </read_first>
  <action>
    Build main defaults and one throwaway channel debug variant by passing Gradle properties for app id, engine id, app name, engine name, and channel code. Install the main default on Xiaomi real device first to prove no production package regression. Then install the channel variant only in a safe test environment if available.

    Validate: app starts, channel header is emitted, engine detection uses configured package, permission center opens the configured engine, engine binds, existing main shop opens, and channel package can coexist without authority collision.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug :Bcore:assembleDebug</automated>
    <manual>Device smoke checklist: install, login, engine status, permission center, shop launch, app/channel header capture.</manual>
  </verify>
  <acceptance_criteria>
    - Main debug build installs and runs without changing production application id.
    - Engine service binding succeeds with configured package.
    - Existing logged-in main shop launches directly without recovery unless recovery is actually needed.
    - A channel debug package can be built from Gradle parameters without editing source.
    - Channel build does not collide with main provider authorities.
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <automated>
    - `./gradlew :app:assembleDebug :Bcore:assembleDebug`
    - `./gradlew :app:processDebugMainManifest :Bcore:processDebugMainManifest`
    - `rg "com\\.zhirang\\.zhanghaoguanjia\\.engine|top\\.niunaijun\\.blackbox|BlackBox" app/src/main/java Bcore/src/main/java`
  </automated>
  <manual>
    - Xiaomi real-device main install: app starts, logs in, engine binds, permission center opens, existing shop launches.
    - Optional channel debug install: channel package coexists with main and uses separate Android data directories.
  </manual>
</verification>

## Artifacts This Phase Produces

- Gradle properties for app package id, engine package id, app name, engine name, and channel code.
- `BuildConfig.APK_CHANNEL` and `BuildConfig.ENGINE_PACKAGE` in the main APK.
- Manifest placeholders for provider authorities, bind permissions, service actions, and installer authorities.
- Central app-side engine package resolver used by installer, connection, loader, and permission center.
- Engine-private, package-aware data root behavior for channel packages.
